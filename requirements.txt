# Core Scientific Computing
numpy>=2.1.0
scipy>=1.14.0
pandas>=2.2.0

# Machine Learning
scikit-learn>=1.5.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0

# Jupyter
jupyter>=1.1.0
ipykernel>=6.29.0
ipywidgets>=8.1.0

# Deep Learning
# PyTorch is the default backend across platforms
torch>=2.5.0

# Apple Silicon MLX backend (macOS only)
# NOTE: Install these only on macOS arm64. The setup script handles this automatically.
mlx>=0.29.0; platform_system == "Darwin" and platform_machine == "arm64"
mlx-lm>=0.28.0; platform_system == "Darwin" and platform_machine == "arm64"

# Development Tools
ipython>=8.29.0
black>=24.10.0
flake8>=7.1.0
pytest>=8.3.0

# Additional utilities
tqdm>=4.67.0
pyyaml>=6.0.2
